/**
 * _deprecated/
 *
 * Graveyard for components that have been superseded but are kept for reference.
 * DO NOT import anything from this folder into production code.
 *
 * Files and their replacements:
 *
 *  SearchBar.jsx        → SearchSection.jsx       (Home page unified search)
 *  FilterBar.jsx        → MultiFilter.jsx          (CategoryFilter + CountryFilter combined)
 *  RecipeCard.jsx       → RecipeImageCard.jsx      (image-first card with lazy loading)
 *  RecipeCategories.jsx → CategoryFilter.jsx       (pill-style category filter inside MultiFilter)
 *  CountryCategories.jsx→ CountryFilter.jsx        (pill-style cuisine filter inside MultiFilter)
 *
 * Safe to delete permanently once backend integration is stable and confirmed working.
 */
