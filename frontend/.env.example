# Taberu — Frontend Environment Template
# Copy this file to .env and fill in values. Never commit .env to git.

# Laravel API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000/api
